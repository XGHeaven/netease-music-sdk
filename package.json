{
  "name": "netease-music-sdk",
  "version": "0.1.1",
  "description": "NetEast Music SDK",
  "main": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "scripts": {
    "test": "jest",
    "build": "tsc",
    "clean": "rimraf lib",
    "prepublish": "npm run clean && npm run build",
    "lint": "tslint -p ."
  },
  "author": "XGHeaven",
  "repository": {
    "type": "git",
    "url": "git@github.com:XGHeaven/netease-music-sdk.git"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/big-integer": "0.0.31",
    "@types/jest": "^22.0.1",
    "@types/node": "^9.3.0",
    "jest": "^22.0.6",
    "rimraf": "^2.6.2",
    "ts-jest": "^22.0.1",
    "tslint": "^5.9.1",
    "typescript": "^2.6.2"
  },
  "dependencies": {
    "axios": "^0.17.1",
    "big-integer": "^1.6.26"
  },
  "jest": {
    "transform": {
      "^.+\\.ts$": "ts-jest"
    },
    "testMatch": [
      "**/*.spec.ts"
    ],
    "moduleFileExtensions": [
      "ts",
      "js",
      "esm",
      "json",
      "node"
    ],
    "testEnvironment": "node"
  }
}
